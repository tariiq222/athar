# المرحلة ٧ — الواجهة الأمامية (Frontend Spec) — أثر

> واجهة العميل الكاملة لمنصّة "أثر" (محتوى عربي). تستهلك خدمات الـbackend عبر عقود REST (NestJS) وتعرضها بتجربة **عربية-أوّل، RTL كامل، موبايل-أوّل**. لا منطق أعمال في الواجهة — التوليد والبحث والفوترة والاستشهاد كلّها خلف الـAPI. تغطّي هذه المرحلة شاشات المراحل ١–٦ بصرياً وتربطها بعقودها. تطبّق حدود المنصات من [15-مواصفات-المنصات.md](../../blueprint/15-مواصفات-المنصات.md) مرئياً عبر `CharCounter`، وتعرض مخرجات محرّك [16-معمارية-المحرّك.md](../../blueprint/16-معمارية-المحرّك.md).
>
> **الإصدار:** V1 (منقّح — نطاق مخفَّض، M2 هو المنتج الحقيقي القابل للتجربة). المنفّذ: Ali. التسليمات الكاملة في القسم ١١.
>
> **Design Context:** [.impeccable.md](../../.impeccable.md) هو الـsource of truth لكل القرارات البصرية. هذا الـspec يستهلكه ولا يعيد تعريفه.

---

## ١. المبادئ

- **عربي-أوّل، RTL كامل:** `dir="rtl"` و`lang="ar"` على الجذر؛ كل تخطيط يُصمّم RTL أصالةً لا كقلب لاحق. خط `IBM Plex Sans Arabic` للجسم، `TheYearofHandicrafts` للعناوين display، `Effra` للـLatin — كلّها في `app/fonts/` داخل المشروع (لا مسارات من جهاز المطوّر).
- **موبايل-أوّل:** التصميم يبدأ من عرض الموبايل ثم يتوسّع؛ أهداف اللمس ≥٤٤px، تنقّل سفلي على الموبايل.
- **لا منطق أعمال في الواجهة:** الواجهة تعرض وتجمع المدخلات وتستدعي العقود فقط. كلّ الحدود والقيود خلف الـbackend.
- **TanStack Query لطبقة البيانات:** استعلام + mutation + caching + invalidation بدون طبقات `services/` و`hooks/` إجبارية. Service wrapper فقط عند الحاجة (انظر القسم ٥).
- **حالات صريحة دائماً:** كل شاشة تعرّف loading/empty/error/success.
- **StatusBadge = المسار الوحيد لعرض الحالة.**
- **Premium without ornament:** نعكس Linear/Arc/Vercel. لا gradient text، لا side-stripe borders، لا glassmorphism، لا hero-metric template.

---

## ٢. التقنية والبنية

- **Next.js 15 (App Router) + React 19 + TypeScript.**
- **التنسيق:** CSS Modules + global tokens في `styles/tokens.css` (OKLCH variables). لا utility-class framework — لا Tailwind. (أزلنا Tailwind بالكامل لتجنّب التشتيت.)
- **الخطوط:** ملفّات في `app/fonts/`:
  - `app/fonts/handicrafts/` (TheYearofHandicrafts TTF/OTF — Reg, Med, SemiBold, Bold, Black)
  - `app/fonts/plex-arabic/` (IBM Plex Sans Arabic — عبر next/font/local)
  - `app/fonts/effra/` (Effra — Reg, Med, SemiBold, Bold + italics + Std)
  - **الترخيص:** TheYearofHandicrafts و Effra يجب التحقق من ترخيصهما قبل التضمين في production. في الـM1 يُضافان كـ assets مع TODO لتعليق الترخيص.
- **الحالة:** TanStack Query لـserver state. Contexts خفيفة: `SessionContext`, `LocaleContext`, `ToastContext`.
- **Auth:** Cookie httpOnly فقط (`SameSite=Lax`, `Secure` في production). الـfrontend يستخدم `credentials: 'include'` ولا يحقن Bearer headers.

### ٢.١ شجرة المجلّدات

```
app/
  (auth)/login/page.tsx
  (auth)/register/page.tsx
  (app)/onboarding/page.tsx
  (app)/dashboard/page.tsx
  (app)/posts/page.tsx
  (app)/posts/[postId]/page.tsx
  (app)/generate/page.tsx
  (app)/billing/page.tsx                  # V1.2
  (app)/calendar/page.tsx                  # V1.1
  (app)/publish/[postId]/page.tsx          # V1.1
  (app)/settings/page.tsx                  # V1.2
  (app)/settings/accounts/page.tsx         # V1.2
  layout.tsx            // dir=rtl, lang=ar, font, providers
fonts/handicrafts/                       # display Arabic
fonts/plex-arabic/                       # body Arabic
fonts/effra/                             # Latin
components/
  shared/               # StatusBadge, PostCard, SourceList, ImagePreview, CharCounter, Toast, Skeleton, EmptyState, ErrorState
  layout/               # AppShell, BottomNav, SideNav, Header
queries/                # TanStack Query hooks (useAuth, usePosts, useGenerate, ...)
lib/                    # apiClient (cookie-based), formatNumber, dir helpers, twitter-text wrapper
i18n/                   # ar (افتراضي), en
types/                  // عقود الـbackend (منسوخة من src/*)
styles/                 // tokens.css (OKLCH vars), globals.css, fonts.css
test/
  unit/                 // Vitest + RTL
  e2e/                  // Playwright
```

### ٢.٢ تدفّق البيانات (TanStack Query فقط)

```
Component  →  useQuery / useMutation (queries/usePosts.ts, ...)
   ↓
Hook       →  fetch('/api/v1/...', { credentials: 'include' })
   ↓
fetch      →  /api/v1/*  (NestJS)
```

- **`apiClient` هو fetch wrapper بسيط** يحقن `credentials: 'include'` ويعالج 401 (logout + redirect `/login` مع حفظ المسار).
- **لا طبقات services/hooks إلزامية.** إذا احتاج endpoint منطقاً مركّباً (مثل `startMonthPlan`)، يُغلَّف في hook مخصّص داخل `queries/`. خلاف ذلك، `useQuery` يكفي.
- **الاستثناء الوحيد:** `apiClient` نفسه لأن 401 handling و base URL و envelope normalization مركزية.

---

## ٣. Design System (المرجع: [.impeccable.md](../../.impeccable.md))

### ٣.١ الألوان (OKLCH tokens في `styles/tokens.css`)

نفس الجدول من النسخة السابقة — primary-deep، primary، surface (cream)، accent (terracotta)، border، heading، mint، sage، muted، elevated. **القاعدة 60-30-10 + ممنوعات ملزمة** سارية.

### ٣.٢ Typography

TheYearofHandicrafts (display) + IBM Plex Sans Arabic (body) + Effra (Latin). 7-step scale، `tnum` للأرقام.

### ٣.٣ Spacing

4pt scale (4/8/12/16/24/32/48/64/96) semantic tokens.

### ٣.٤ Motion

5 timing tokens + ease-out-expo + reduced-motion.

### ٣.٥ المكوّنات المشتركة (V1: فقط ما يُستخدم فعلياً)

| مكوّن | يُستخدم في | ملاحظة |
|---|---|---|
| `StatusBadge` | Dashboard, Posts list, Editor | المسار الوحيد لعرض حالة البوست |
| `EmptyState` | Posts list, Dashboard | عربي + CTA تعليمي |
| `ErrorState` | كل مكان | عربي + "أعد المحاولة" |
| `Skeleton` | كل مكان loading | هيكل صامت |
| `PostCard` | Dashboard, Posts list | موبايل-أوّل، لا border-left |
| `SourceList` | Editor | flat list، روابط rel=noopener |
| `ImagePreview` | Editor | overlay fallback |
| `CharCounter` | Editor | LinkedIn ٣٬٠٠٠؛ X بـ twitter-text |
| `Toast` | عام | إشعارات |
| `AppShell`, `BottomNav`, `SideNav` | كل صفحة `(app)` | mobile BottomNav ≥ 4 وجهات |

**Layout primitives:** AppShell + BottomNav (4 destinations: Dashboard, Generate, Posts, Profile) + SideNav (≥md).

---

## ٤. النطاق — V1 مخفَّض (M1 + M2 فقط)

> **الفلسفة:** V1 = منتج قابل للتجربة يثبت أن المستخدم سيدفع. M2 = التدفّق الأساسي Onboarding → Generate → Edit → Approve. Calendar/Publish/V1.1، Billing/V1.2، Settings/V1.2.

### M1 — الأساسيات (الـPR الأول)

**الهدف:** next.js قابل للتشغيل، design system tokens، Auth، AppShell، Dashboard فارغ.

| المسار | الدور |
|---|---|
| `/login` | بريد + كلمة مرور → cookie → dashboard |
| `/register` | بريد + كلمة مرور + `acceptTerms` + `termsVersion` → cookie → onboarding |
| `/dashboard` | قشرة فارغة: "مرحباً {user.name}" + عدد البوستات (0 في البداية) + CTA "أكمل دماغ الشركة" |

**المخرجات في M1:**
- Next.js 15 + React 19 + TS + ESLint + Stylelint + Vitest + RTL + Playwright
- `styles/tokens.css` + `globals.css` + `fonts.css` كاملة (OKLCH)
- خطوط منسوخة إلى `app/fonts/`
- `app/layout.tsx` (dir=rtl، lang=ar، font loading)
- `components/shared/`: StatusBadge، EmptyState، ErrorState، Skeleton
- `components/layout/`: AppShell، BottomNav، SideNav
- `lib/apiClient.ts` (cookie-based + 401)
- `lib/queryClient.ts` (TanStack Query setup)
- `i18n/ar.json`, `i18n/en.json` + `LocaleContext`
- `queries/useAuth.ts` (login/register/logout)
- الشاشات: `/login`, `/register`, `/dashboard` (قشرة)
- JWT cookie guard على `(app)`
- Stylelint: قواعد anti-pattern (no border-left > 1px، no raw color values)
- Tests: Vitest + RTL للمكوّنات + Playwright E2E login/logout

### M2 — الـMVP الحقيقي (الـPR الثاني)

**الهدف:** Onboarding → Generate → Edit → Approve. هذا ما يثبت قيمة "أثر".

| المسار | الدور |
|---|---|
| `/onboarding` | Wizard 5 خطوات (مصدره `POST /brand/analyze` → أسئلة → اعتماد `POST /brand/profile`) |
| `/dashboard` | محسَّن: عدد البوستات بكل حالة، أقرب المجدولة، CTA "ولّد بوست" و"خطة شهر" |
| `/posts` | قائمة `PostCard` + تصفية حالة/منصة |
| `/posts/[postId]` | محرّر: نص + `SourceList` + `ImagePreview` + `CharCounter` + اعتماد + إعادة توليد |
| `/generate` | مفرد + خطة شهر (polling عبر TanStack Query) |

**المخرجات في M2:**
- `queries/useBrandProfile.ts`, `useBrandAnalysis.ts`
- `queries/usePosts.ts`, `usePost.ts`, `useApprovePost.ts`
- `queries/useGeneration.ts`, `useMonthPlan.ts` (with polling)
- `components/shared/`: PostCard، SourceList، ImagePreview، CharCounter
- 5 شاشات جديدة
- Tests: Playwright رحلة كاملة تسجيل → onboarding → توليد → اعتماد

### V1.1 — Calendar + Publish (الـPR الثالث)

**الهدف:** ربط المحتوى بالتقويم السعودي + النشر اليدوي.

| المسار | الدور |
|---|---|
| `/calendar` | شبكة/قائمة أيام، المناسبات السعودية خلفية، البوستات داخل الأيام، **اختيار تاريخ + زر "أعد الجدولة"** (لا drag-and-drop) |
| `/publish/[postId]` | نسخ النص + تنزيل الصورة + فتح المنصة + "تم النشر" |

**خارج M2:** drag-and-drop، تقويم أسبوعي/يومي، reminders.

### V1.2 — Billing + Settings + Accounts (بعد Sprint A)

| المسار | اعتماد |
|---|---|
| `/billing` | ⛳ Sprint A (VAT + cancel-at-period-end + HMAC) |
| `/settings` | — |
| `/settings/accounts` | — |

> **القاعدة:** أي شاشة من V1.1/V1.2 في كود الـM2 ⇒ كسر القاعدة. الـM2 PR يجب أن يكون self-contained.

---

## ٥. استهلاك الـAPI

```ts
// lib/apiClient.ts
export const apiClient = {
  async get<T>(path: string): Promise<T> {
    const res = await fetch(`${API_BASE}${path}`, { credentials: 'include' })
    if (res.status === 401) { /* logout + redirect /login */ }
    if (!res.ok) throw await toApiError(res)
    return res.json() as Promise<T>
  },
  post: /* same shape */,
  patch: /* same shape */,
  delete: /* same shape */,
}
```

### استعلامات V1 (TanStack Query)

| Endpoint | Query key | Hook |
|---|---|---|
| `POST /auth/login` | — | `useLogin()` (mutation) |
| `POST /auth/register` | — | `useRegister()` (mutation) |
| `POST /auth/logout` | — | `useLogout()` (mutation) |
| `POST /brand/analyze` | — | `useAnalyzeBrand()` (mutation) |
| `GET /brand/profile` | `['brand', 'profile']` | `useBrandProfile()` |
| `POST /brand/profile` | — | `useCreateProfile()` (mutation) |
| `PATCH /brand/profile/:id` | — | `useUpdateProfile()` (mutation) |
| `GET /posts` | `['posts', filters]` | `usePosts(filters)` |
| `GET /posts/:id` | `['post', id]` | `usePost(id)` |
| `PATCH /posts/:id` | — | `useUpdatePost()` (mutation) |
| `POST /content/generate` (engine) | — | `useGenerate()` (mutation) |
| `POST /content/month-plan` | — | `useStartMonthPlan()` (mutation) |
| `GET /content/month-plan/:jobId` | `['month-plan', jobId]` | `useMonthPlan(jobId)` (polling) |

> كل mutation يبطل الـquery keys المرتبطة (e.g. `useUpdatePost` → `['post', id]` + `['posts']`).

### Auth (Cookie httpOnly)

- **لا JWT في localStorage. لا Bearer headers.** Cookie httpOnly فقط.
- `apiClient` يستخدم `credentials: 'include'` على كل طلب.
- `useAuth` يقرأ cookie state عبر endpoint آمن `GET /auth/me` (يُضاف في الـbackend إن لم يكن موجوداً).
- 401 → حذف state + redirect `/login` مع حفظ المسار.
- آلية الجلسة: cookie session مع access + refresh كزوج cookies، rotation عبر `POST /auth/refresh`.

---

## ٦. التدفّق والحالات

| الموضع | الحالة | السلوك |
|---|---|---|
| Onboarding | غير مكتمل | `/dashboard` يعرض CTA → `/onboarding` يستأنف من آخر خطوة |
| قائمة البوستات | لا بوستات | EmptyState عربي + CTA "ولّد أول بوست" |
| التوليد المفرد | فشل | ErrorState عربي + "أعد المحاولة"؛ المدخلات محفوظة |
| خطة الشهر | بوست فاشل ضمن الخطة | StatusBadge=failed + "أعد توليد هذا"؛ بقية الخطة سليمة |
| محرّر البوست | نقد لم يجتَز بعد ٣ دورات | أفضل نسخة + `issues` كتنبيه شفّاف |
| الصورة | فشل توليد/تكسّر | ImagePreview يعرض overlay fallback + "نص فقط" بعلامة |
| المصادقة | 401/انتهاء cookie | Logout → `/login` مع حفظ المسار |
| أي جلب | تحميل | Skeleton |
| أي جلب | خطأ شبكة | ErrorState عربي + "أعد المحاولة" |

**القاعدة:** كل شاشة تعرّف الحالات الأربع صراحةً.

---

## ٧. RTL و i18n

نفس النسخة السابقة: `<html dir="rtl" lang="ar">`، مفاتيح en + قيم ar، أيقونات اتجاهية معكوسة، focus order RTL، `Asia/Riyadh`.

---

## ٨. إمكانية الوصول (WCAG 2.1 AA + AAA انتقائي)

**V1 = AA إلزامي + AAA للحالات الحرجة:**

| الموضع | المستوى | السبب |
|---|---|---|
| جسم البوست المعتمد | AAA | المحتوى الذي ينشره المستخدم |
| مبالغ وتواريخ الفوترة | AAA | دقة مالية |
| `StatusBadge` labels | AAA | قرار عملي بناءً على الحالة |
| رسائل الخطأ | AAA | المستخدم يتخذ إجراء |
| باقي النصوص | AA | افتراضي V1 |
| `prefers-reduced-motion` | AA | honor دائماً |

**القواعد الملزمة (AA):**
- Contrast 4.5:1 normal text، 3:1 large.
- Color ليس الإشارة الوحيدة (StatusBadge = لون + نص + أيقونة).
- Focus rings 2px primary + 2px offset.
- `aria-live` على التقدّم غير المتزامن.
- Form errors: `aria-describedby` على كل input.
- Touch targets ≥ 44×44px.
- RTL focus mirroring طبيعي.

---

## ٩. استراتيجية الاختبار

- **Vitest + RTL** لكل مكوّن shared وكل query hook. تغطية السلوك (loading/empty/error/success) + ARIA.
- **MSW** mocks في اختبارات الوحدة.
- **Playwright** E2E:
  - رحلة M2 الكاملة: تسجيل → onboarding → توليد → اعتماد
  - تبديل RTL/LTR
  - axe-core audit على الصفحات الرئيسية
- **معيار:** كل شاشة لها على الأقل اختبار سلوك + empty + error. لا 80% تغطية إجباري.

---

## ١٠. معايير القبول (DoD مصنَّف)

### Must Have (قبل أي launch)

- [ ] `dir="rtl"` / `lang="ar"` على الجذر؛ التخطيطات منطقية (start/end)
- [ ] خطوط: TheYearofHandicrafts (display) + IBM Plex Sans Arabic (body) + Effra (Latin) — كلّها محمّلة من `app/fonts/`
- [ ] كل النصوص الظاهرة عربية من `i18n/ar.json`؛ لا نص عربي داخل الكود
- [ ] المعرّفات/المسارات/الأنواع/الـhooks بالإنجليزية
- [ ] `StatusBadge` هو المسار الوحيد لعرض حالة البوست، مع accessible name عربي
- [ ] `CharCounter` يطابق حدود ملف ١٥ بصرياً (X بـ twitter-text، LinkedIn ٣٬٠٠٠)
- [ ] Auth: cookie httpOnly فقط، `credentials: 'include'`
- [ ] كل شاشة تعرّف loading/empty/error/success
- [ ] WCAG 2.1 AA على كل الشاشات
- [ ] tokens.css (OKLCH) + Stylelint يمنع border-left > 1px + raw colors
- [ ] TanStack Query: cache keys + invalidation
- [ ] رحلة E2E M2 (تسجيل → onboarding → توليد → اعتماد) تعمل في Playwright

### Should Have (في V1 لكن ليس blocker)

- [ ] AAA contrast على جسم البوست المعتمد + مبالغ الفوترة + StatusBadge + أخطاء
- [ ] axe-core audit ينظف (0 violations)
- [ ] Reduced motion honored
- [ ] Tab order RTL صحيح في كل النماذج
- [ ] M3 (Calendar + Publish) — V1.1
- [ ] A11y keyboard pass كامل

### Later (V1.2+ — بعد Sprint A وإثبات الاستخدام)

- [ ] `/billing` — يعتمد Sprint A
- [ ] `/settings` + `/settings/accounts`
- [ ] Drag-and-drop في التقويم
- [ ] i18n كامل (en محتوى حقيقي، ليس فقط مفاتيح)
- [ ] dark mode (لا V1 — فاتح فقط حسب .impeccable.md)
- [ ] Multiple plans / upgrade / downgrade (V2)
- [ ] Auto-publish (V2)
- [ ] Analytics (V2)

---

## ١١. Backend Gaps (يجب سدّها قبل M2)

اكتُشفت أثناء pre-flight على `main@093a082`. الواجهة تعتمد على هذه العقود، والـbackend لا يوفّرها بعد:

| Endpoint متوقّع | الحالة في الـbackend | ملاحظة |
|---|---|---|
| `POST /content/generate` | **غير موجود** — `PipelineService` متاح داخلياً فقط | يحتاج `EngineController` جديد يلفّ `pipeline.generateOne` |
| `POST /content/month-plan` | **غير موجود** — `MonthPlanService` متاح داخلياً فقط | يحتاج endpoint يبدأ الـBullMQ job ويعيد `{ jobId }` |
| `GET /content/month-plan/:jobId` | **غير موجود** | يحتاج endpoint لقراءة تقدّم الـjob |
| `GET /auth/me` | **غير موجود** — الـfrontend يحتاجه للتحقّق من الـcookie session | إضافة بسيطة إلى `AuthController` |

**الإجراء المطلوب قبل M2:** backend engineer (Tariq) يُضيف `EngineController` (`src/engine/engine.controller.ts`) + `GET /auth/me`. هذا scope خارج نطاق V1 frontend لكنه blocker.

---

## ١٢. تسليمات Ali

1. **`.impeccable.md`** — design context (شخصيات، palette، typography، principles، anti-patterns، AA+AAA accessibility).
2. **`docs/specs/المرحلة-7-الواجهة.md`** — هذا الـspec (V1 المنقّح).
3. **`docs/superpowers/plans/2026-06-30-phase7-frontend.md`** — خطة تنفيذ تفصيلية لكل milestone مع TDD tasks (يُكتب في writing-plans).
4. **`styles/tokens.css` + `globals.css` + `fonts.css`** — starter كامل مع كل OKLCH variables.
5. **`app/fonts/`** — ملفّات الخطوط منسوخة من `/Users/tariq/Desktop/خط عام الحرف اليدوية 2025/TTF/` و `/Users/tariq/Downloads/Munasabat-00016 2/خط افرا/` (مهمة setup واحدة في M1). **الترخيص:** TODO صريح قبل تضمين TheYearofHandicrafts في production.
6. **`components/shared/` skeleton** — TypeScript interfaces فقط لكل مكوّن (props + types)، التنفيذ لـ Ali.
7. **`queries/` skeleton** — useQuery/useMutation placeholders للـV1 endpoints فقط.
8. **`lib/apiClient.ts` + `lib/queryClient.ts`** — implementations كاملة مع cookie handling.
9. **`package.json` starter** — Next 15, React 19, TypeScript, Vitest, RTL, MSW, Playwright, axe-core, TanStack Query, Stylelint.
10. **`stylelint.config.js`** — تمنع border-left > 1px، raw color values، raw pixel values في spacing.
11. **`stylelint.config.js`** + **`eslint.config.js`** — صارمة.

---

## ١٣. خارج النطاق

- **منطق الأعمال كلّه في الـbackend.** الواجهة تستدعي فقط.
- **النشر الآلي، Auto-publish:** V2.
- **الدفع (Moyasar):** منطق الدفع والـwebhook في الـbackend. الواجهة V1.2 فقط.
- **تحليلات، أتمتة، white-label، video، multi-platform، باقات متعدّدة:** V2.
- **Native (iOS/Android):** غير مشمول.
- **dark mode:** غير مشمول (فاتح فقط).

---

## ١٤. ملخّص النطاق (نطاق V1 مقابل المؤجَّل)

| الميزة | V1 | V1.1 | V1.2 | V2 |
|---|---|---|---|---|
| Login + Register + Cookie auth | ✅ | | | |
| AppShell + Tokens + RTL + i18n | ✅ | | | |
| Dashboard (قشرة + محسَّن) | ✅ | | | |
| Onboarding wizard | ✅ | | | |
| Generate (مفرد + شهر) | ✅ | | | |
| Posts list + Editor + اعتماد | ✅ | | | |
| StatusBadge + PostCard + SourceList + ImagePreview + CharCounter | ✅ | | | |
| Calendar (لا drag-drop) | | ✅ | | |
| Publish manual | | ✅ | | |
| Billing | | | ✅ | |
| Settings + Accounts | | | ✅ | |
| Drag-drop calendar | | | | ✅ |
| Auto-publish | | | | ✅ |
| Analytics | | | | ✅ |
| dark mode | | | | ✅ |
| Multiple plans | | | | ✅ |